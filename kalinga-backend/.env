# Database Connection (Neon serverless postgres connection string or 'mock' for local JSON db)
DATABASE_URL=mock

# JWT Configuration
JWT_SECRET=super_secret_signing_key_change_me_in_production

# Server Port
PORT=3001

# Environment
NODE_ENV=development
